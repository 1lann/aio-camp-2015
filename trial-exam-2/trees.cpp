#include <iostream>

int main() {
	int n, m;
	cin >> n >> m;

	int treeHeights[n];

	for (int i = 0; i < n; i++) {
		cin >> treeHeights[i];
	}

	for (int i = 0; i < m; i++) {
		char inputTask;
		cin >> inputTask;
		if (inputTask == 'F') {
			int numSmallest, min
		} else if (inputTask == 'C') {

		}
	}
}